@page
@model AOS.Pages.Exams.DeleteModel

@{
    ViewData["Title"] = "Delete";
}


<div class="container">
    <h3>Вы действительно хотите удалить данный экзамен?</h3>
    <div>
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Exam.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Exam.Name)
            </dd>
        </dl>
    
        <form method="post">
            <input type="hidden" asp-for="Exam.Id" />
            <a asp-page="./Details" asp-route-id="@Model.Exam.Id" class="btn btn-secondary mr-3">Назад</a>
            <input type="submit" value="Удалить" class="btn btn-danger" />
        </form>
    </div>
</div>
